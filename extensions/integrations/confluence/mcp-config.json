{
  "_comment": "Confluence uses the same mcp-atlassian server as Jira. If you are using both integrations, you only need to configure the MCP server once. See extensions/integrations/jira/mcp-config.json — it covers both Jira and Confluence.",
  "_reference": "extensions/integrations/jira/mcp-config.json"
}
