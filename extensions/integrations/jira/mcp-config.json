{
  "_comment": "Jira/Confluence MCP server configuration snippet. This repo defaults to Atlassian's official remote MCP. Merge this into your IDE's MCP config file or let setup.sh generate it.",
  "mcpServers": {
    "atlassian": {
      "type": "http",
      "url": "https://mcp.atlassian.com/v1/sse",
      "headers": {
        "Authorization": "Bearer ${ATLASSIAN_API_TOKEN}"
      },
      "disabled": false,
      "autoApprove": [
        "jira_get_issue",
        "jira_search_issues",
        "confluence_get_page",
        "confluence_search"
      ]
    }
  }
}
